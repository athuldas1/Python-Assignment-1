# Python Assignment 1 – Data Structures

## 📌 Overview

This assignment focuses on the basics of **Python Data Structures**, mainly **Strings and Tuples**.

The assignment was completed using **Google Colab**, where the Python code was written, executed, and tested.

## 🚀 Google Colab Notebook

You can view and run the complete assignment in Google Colab:

👉 https://colab.research.google.com/drive/1DK270Ec-gw-EZeVs9cZG33anZNYad7Pt?usp=sharing

> Replace `PASTE_YOUR_GOOGLE_COLAB_LINK_HERE` with your actual Google Colab sharing link.

## 📚 Topics Covered

### 1. String Concatenation

In this section, I practiced joining strings together using the `+` operator.

* Taking the user's name as input
* Combining `"Hello "` with the name
* Adding `", welcome to Python programming"` to the existing string

### 2. String Slicing and Indexing

This section covers accessing and extracting different parts of a string.

* Printing the first character
* Printing the last character
* Printing the first 5 characters
* Printing the last 11 characters
* Reversing the string
* Extracting the word `"Python"` using slicing

### 3. String Methods

I practiced some commonly used Python string methods using:

```python
strM = "Python beginner tutorial"
```

The methods covered are:

* `upper()` – converts the string to uppercase
* `lower()` – converts the string to lowercase
* `capitalize()` – capitalizes the first character
* `count()` – counts the occurrences of a character
* `replace()` – replaces one piece of text with another

### 4. Tuples

This section introduces basic tuple operations.

Two tuples were created:

```python
t1 = (10, 20, 30)
t2 = (40, 50, 60)
```

The following operations were performed:

* Concatenating two tuples
* Repeating tuple elements 3 times
* Accessing the 3rd element
* Accessing the first three elements
* Accessing the last three elements

##
